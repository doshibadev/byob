# Python 3.11+ requirements - no version pins for flexibility and compatibility

mss                  # Screenshot tool, actively maintained
WMI                  # Windows Management Instrumentation Python wrapper (windows only)
numpy                # Core scientific computing library
pynput               # Replacement for pyxhook, actively maintained for input hooks/keylogging
twilio               # Twilio API client
colorama             # Cross-platform colored terminal text
requests             # HTTP requests library
PyInstaller          # For packaging Python apps
opencv-python        # OpenCV bindings, computer vision library

# Windows-specific package for Windows API access
pywin32; sys_platform == "win32"
